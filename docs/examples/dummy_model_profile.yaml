version: 1

id: dummy_model
name: Dummy Model
description: Deterministic dummy model profile for MVP smoke tests.

runtime:
  default: dummy_runtime
  supported:
    - dummy_runtime

input_schema:
  required:
    image:
      dtype: float32
      shape: [1, 3, 64, 64]
    proprio:
      dtype: float32
      shape: [1, 2]
    instruction:
      dtype: string
      shape: scalar

output_schema:
  required:
    action:
      dtype: float32
      shape: [1, 2]

frequency:
  target_hz: 10

latency:
  expected_ms: 1

artifact:
  type: none

warmup:
  sample: generated
